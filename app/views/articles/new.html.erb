<div class="new">
  <h1>New Article</h1>
  <div class="form_group">
    <%= form_for [@user, @article] do |f| %>
      <%= f.text_field :title, placeholder: "Title" %>
      <%= f.text_area :body %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
